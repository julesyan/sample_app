<% provide(:title, "Sign in") %>
<h1>Sign in</h1>

<div class="row">
	<div class="span6 offset3">
		<!--Again using form_for (same as the sign up page in new.html.erb)
			we will create a form for the user to enter information. This time
			we need to indicate the name of the resource and the corresponding
			URL (since this is a session). We can also use form_tag but this
			is a more common method. 
			The HTML code that is generated by these helpers auto fill the 
			id, name, type, and for (lablels use the for attribute which is 
			given the value of the id of the corresponding input
			Submitting this form gives a params hash where its 
			params[:session][:UserAttribute] -->
		<%= form_for(:session, url: sessions_path) do |f| %>

		  <%= f.label :email %>
		  <%= f.text_field :email %>

		  <%= f.label :password %>
		  <%= f.password_field :password %>

		  <%= f.submit "Sign in", class: "btn btn-large btn-primary" %>
		<% end %>

		<!-- This link is for convience -->
		<p>New user? <%= link_to "Sign up now!", signup_path %></p>
	</div>
</div>
